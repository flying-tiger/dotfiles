[alias]
	ci  = commit
	co  = checkout
	dt  = difftool
	lg  = log --graph --oneline --decorate --all
	lsb = branch -a
	lsr = remote -v show
	st  = status
	super-clean = clean -f -d -x

[core]
	editor = vim

[diff]
	tool = gvimdiff

[difftool]
	prompt = false

[push]
	default = simple
